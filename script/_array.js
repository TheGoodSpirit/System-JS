const friends = ["Ram", "Hari"]
console.log(`Array = ${friends}`)

// add data to the array
friends.push("Shyam");  // Adds 'Shyam' at the end of the array
console.log(`Push = ${friends}`)

friends.push("Geeta");  // Adds 'Shyam' at the end of the array
console.log(`Push adding = ${friends}`)

friends.unshift("David");  // Adds 'Shyam' at the end of the array
console.log(`Unshift = ${friends}`)

friends.unshift("Ayush");  // Adds 'Shyam' at the end of the array
console.log(`Unshift = ${friends}`)

// delete data from array
friends.pop();
console.log(`Pop = ${friends}`)

friends.pop();
console.log(`Pop = ${friends}`)

friends.shift();
console.log(`Pop = ${friends}`)

friends.shift();
console.log(`Pop = ${friends}`)

